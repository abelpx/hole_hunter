# HoleHunter 数据库配置示例
# 复制此文件为 .env 或 .env.local 并修改配置

# ==================== 数据库配置 ====================

# 数据库类型: sqlite | mysql | mariadb | postgresql
# 桌面端默认使用 sqlite，Web 端可以使用 mysql/mariadb/postgresql
HOLEHUNTER_DB_TYPE=sqlite

# ==================== SQLite 配置（桌面端默认）====================
# SQLite 数据库文件路径（可选，默认使用用户数据目录）
# DATABASE_PATH=/path/to/holehunter.db

# ==================== MySQL/MariaDB 配置（Web 端）====================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=holehunter
DB_USER=root
DB_PASSWORD=your_password_here

# 连接池配置
DB_POOL_MIN=2
DB_POOL_MAX=10

# ==================== PostgreSQL 配置（Web 端）====================
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=holehunter
# DB_USER=postgres
# DB_PASSWORD=your_password_here
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# ==================== Nuclei 配置 ====================
# Nuclei CLI 路径（可选，默认使用系统 PATH）
NUCLEI_PATH=nuclei

# Nuclei 模板目录（可选）
NUCLEI_TEMPLATES_PATH=./nuclei-templates

# 默认扫描速率限制（请求/秒）
NUCLEI_DEFAULT_RATE_LIMIT=150

# 默认超时时间（秒）
NUCLEI_DEFAULT_TIMEOUT=10

# 默认重试次数
NUCLEI_DEFAULT_RETRIES=1
